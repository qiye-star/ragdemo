# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# 环境与密钥
.env
.env.local
*.pem
*.key

# Superpowers SDD 工作区（临时产物，非仓库内容）
.superpowers/

# 数据与运行时产物
# 注意：db/seed/*.csv 是**提交进仓库**的。P0 验收（docs/10-roadmap.md）要求
# 在干净 clone 上 `make seed` 就能复现四张表的计数，忽略它等于让验收不可复现。
data/
*.log
